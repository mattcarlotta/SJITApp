import { sendError } from "shared/helpers";

const createTeam = (req, res, done) => sendError("Route not setup.", res, done);

const deleteTeam = (req, res, done) => sendError("Route not setup.", res, done);

const getAllTeams = (req, res, done) => sendError("Route not setup.", res, done);

const getTeam = (req, res, done) => sendError("Route not setup.", res, done);

const updateTeam = (req, res, done) => sendError("Route not setup.", res, done);

export {
  createTeam, deleteTeam, getAllTeams, getTeam, updateTeam,
};
